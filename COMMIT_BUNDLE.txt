TÀNG THƯ commit bundle — 2026-09-16

Copy these files into the repository, preserving paths:

cmd/generate/main.go
.github/workflows/register-shelf.yml
GITHUB_PAGES.md

Do not commit this manifest unless desired; it is included only to make the bundle self-documenting.

Main changes:
- Tủ sách modal: one check button + one dynamic action button (Tạo tủ / Đổi tên / Xóa tủ).
- Action opens a prefilled GitHub Issue and closes the modal.
- Registration workflow handles create / rename / delete.
- Registration workflow explicitly dispatches pages.yml after config change.
- GITHUB_PAGES.md records the code handoff/editing rule.
